<h1>Contact Index</h1>

<% @contacts.each do |contact| %>
  <p><%= link_to(contact.name, contact_path(contact.id)) %> - <%= contact.message%></p>
<% end %>

<p><%= link_to("New Contact", new_contact_path) %></p>
<p><%= link_to("Home", root_path) %></p>